# Firebase Configuration (shared with Gastify — same Firebase project)
# Local dev (.env) → staging project (boletapp-staging)
# Production (.env.production) → production project (boletapp-d609f)
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# E2E Testing — set to "true" to expose auth helpers on window for Playwright
# VITE_E2E_MODE=true

# Cloud Functions — Gemini API key is stored in functions/.env (not here)
# See functions/.env.example for the GEMINI_API_KEY setup
